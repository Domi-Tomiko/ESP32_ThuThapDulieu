# cmake_minimum_required(VERSION 3.22)

# include($ENV{IDF_PATH}/tools/cmake/project.cmake)

# littlefs_create_partition_image(storage data FLASH_IN_PROJECT)

# project(esp32_camera_server)

cmake_minimum_required(VERSION 3.22)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)

project(esp32_camera_server)